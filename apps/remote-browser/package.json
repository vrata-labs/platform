{
  "name": "@noah/remote-browser",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@noah/shared-types": "workspace:*",
    "playwright-core": "^1.58.2",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  },
  "scripts": {
    "dev": "node --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "NOAH_DISABLE_AUTOSTART=1 node --test dist/**/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
